name: WORLD v0.56 Araki Hessian Physical Realization macOS

on:
  push:
    branches:
      - world-v056-macos-ci-v2
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: WORLD v0.56 Araki Hessian Physical Realization macOS-791
  cancel-in-progress: true

jobs:
  release-runner:
    runs-on: macos-latest
    steps:
      - run: echo 'Superseded by current product head validation in PR 794.'
