name: Qi Wuxing AddCon First Isomorphism v2.9

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'docs/KUUOS_QI_WUXING_ADDCON_FIRST_ISOMORPHISM_v2_9.md'
      - 'formal/KUOS/Architecture/QiWuxingAddConFirstIsomorphismV2_9.lean'
      - 'formal/KuuOSQiWuxingAddConFirstIsomorphismV2_9.lean'
      - '.github/workflows/qi-wuxing-addcon-first-isomorphism-v2-9-validation.yml'
  pull_request:
    paths:
      - 'docs/KUUOS_QI_WUXING_ADDCON_FIRST_ISOMORPHISM_v2_9.md'
      - 'formal/KUOS/Architecture/QiWuxingAddConFirstIsomorphismV2_9.lean'
      - 'formal/KuuOSQiWuxingAddConFirstIsomorphismV2_9.lean'
      - '.github/workflows/qi-wuxing-addcon-first-isomorphism-v2-9-validation.yml'

permissions:
  contents: read

concurrency:
  group: qi-wuxing-addcon-first-isomorphism-v29-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  MATHLIB_NO_CACHE_ON_UPDATE: '1'

jobs:
  lean:
    name: Focused strict Lean
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
      - name: Verify package-local formal boundary
        run: |
          set -euo pipefail
          grep -Fq \
            'import KUOS.Architecture.QiWuxingPhaseProjectionQuotientKernelV2_8' \
            formal/KUOS/Architecture/QiWuxingAddConFirstIsomorphismV2_9.lean
          grep -Fq \
            'import KUOS.Architecture.QiWuxingAddConFirstIsomorphismV2_9' \
            formal/KuuOSQiWuxingAddConFirstIsomorphismV2_9.lean
      - name: Set up pinned Lean and Lake
        uses: leanprover/lean-action@v1
        with:
          auto-config: false
          build: false
          test: false
          lint: false
          use-mathlib-cache: false
          use-github-cache: false
      - name: Restore Lean dependency and build cache
        id: lean-cache
        uses: actions/cache@v5
        with:
          path: |
            .lake/packages
            .lake/build
          key: kuuos-lean-v2-${{ runner.os }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}
      - name: Resolve dependencies on cache miss
        if: steps.lean-cache.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          for attempt in 1 2 3; do
            if lake update; then
              break
            fi
            if [ "$attempt" -eq 3 ]; then
              exit 1
            fi
            sleep $((attempt * 10))
          done
          git diff --exit-code -- lake-manifest.json
          lake exe cache get
      - name: Strictly compile only the v2.9 module
        run: |
          set -euo pipefail
          lake -KleanArgs=-DwarningAsError=true \
            -KleanArgs=-DsorryAsError=true \
            build KUOS.Architecture.QiWuxingAddConFirstIsomorphismV2_9
