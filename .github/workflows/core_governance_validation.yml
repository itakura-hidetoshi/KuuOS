name: Core Governance Validation (consolidated)

on:
  workflow_dispatch:

jobs:
  consolidated:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Consolidated workflow notice
        run: echo "This validation is consolidated into all_governance_validation.yml."
