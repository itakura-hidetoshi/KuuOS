name: KuuOS v0.77 Checks

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  runtime:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
      - run: python3 --version
