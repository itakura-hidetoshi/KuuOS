name: PlanOS v0.8 Validation
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m compileall -q runtime scripts/check_plan_os_reentry_ownership_continuity_v0_8.py
      - run: python scripts/check_plan_os_reentry_ownership_continuity_v0_8.py
