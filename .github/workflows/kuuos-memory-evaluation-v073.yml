name: KuuOS Memory Evaluation v0.73

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

env:
  MATHLIB_NO_CACHE_ON_UPDATE: '1'

jobs:
  validate-runtime:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Compile runtime
        run: >-
          python3 -m compileall -q
          runtime/kuuos_memory_family_v0_73.py
          runtime/kuuos_memory_policy_v0_73.py
          runtime/kuuos_memory_assessment_v0_73.py
          runtime/kuuos_memory_score_v0_73.py
          runtime/kuuos_memory_selection_v0_73.py
          runtime/kuuos_memory_family_evaluation_v0_73.py
          scripts/check_kuuos_memory_evaluation_v073.py
      - name: Validate memory evaluation
        run: PYTHONPATH=. python3 scripts/check_kuuos_memory_evaluation_v073.py
      - name: Validate manifest
        run: python3 -m json.tool manifests/kuuos_memory_evaluation_v0_73.json >/dev/null

  validate-formal:
    runs-on: ubuntu-22.04
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v6
      - name: Set up Lean
        uses: leanprover/lean-action@v1
        with:
          auto-config: false
          build: false
          test: false
          lint: false
          use-mathlib-cache: false
          use-github-cache: false
      - name: Resolve dependencies
        run: lake update
      - name: Verify manifest stability
        run: git diff --exit-code -- lake-manifest.json
      - name: Fetch Mathlib cache
        run: lake exe cache get
      - name: Build aggregate and v0.73 roots
        run: >-
          lake -KleanArgs=-DwarningAsError=true
          -KleanArgs=-DsorryAsError=true
          build KuuOSFormal KuuOSMemoryEvaluationV0_73
