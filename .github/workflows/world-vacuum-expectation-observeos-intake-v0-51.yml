name: WORLD Vacuum Expectation ObserveOS Intake v0.51

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'README.md'
      - 'ROADMAP.md'
      - 'formal/KUOS/WORLD/VacuumExpectationObserveOSEvidenceIntakeBridgeV0_51.lean'
      - 'formal/KuuOSFormalV0_51.lean'
      - 'formal/KUOS.lean'
      - 'lakefile.toml'
      - 'manifests/world_vacuum_expectation_observeos_evidence_intake_v0_51.json'
      - 'scripts/check_world_vacuum_expectation_observeos_evidence_intake_v0_51.py'
      - 'scripts/run_kuuos_runtime_full_check_v0_51.py'
      - 'docs/KU_WORLD_VACUUM_EXPECTATION_OBSERVEOS_EVIDENCE_INTAKE_v0_51.md'
      - '.github/workflows/world-vacuum-expectation-observeos-intake-v0-51.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run v0.51 static validation
        run: python3 scripts/check_world_vacuum_expectation_observeos_evidence_intake_v0_51.py
      - name: Set up pinned Lean and Lake
        uses: leanprover/lean-action@v1
        with:
          auto-config: false
          build: false
          test: false
          lint: false
          use-mathlib-cache: false
          use-github-cache: false
      - name: Resolve pinned dependencies
        run: lake update
      - name: Verify committed manifest is canonical
        run: git diff --exit-code -- lake-manifest.json
      - name: Fetch Mathlib olean cache
        run: lake exe cache get
      - name: Compile WORLD v0.51 root
        run: >-
          lake -KleanArgs=-DwarningAsError=true
          -KleanArgs=-DsorryAsError=true
          build KuuOSFormalV0_51
