# MemoryOS v0.82 — Global Word Sections and Anchor-Fiber Equivalence

## Scope

MemoryOS v0.82 advances the normalized four-generator pair-groupoid model of v0.81 to a root-independent global-section layer.

The source remains the finite four-route atlas and exact `S3` certificate chain. No continuum sheaf classification, universal free-groupoid presentation, physical gauge-field inference, candidate ranking, decision authority, activation, execution, or truth authority is introduced.

## Global normalized word section

Let each route root `i` carry the normalized free group

`F_i = F(Y_1,Y_2,Y_3,D)`.

The exact v0.81 transport from the target fiber `F_j` to the source fiber `F_i` is

`T_i<-j : F_j -> F_i`.

A global word section is a family `s = (s_i)` satisfying

`T_i<-j(s_j) = s_i`

for every ordered root pair.

## Extension from one anchor

For an anchor root `a` and word `w` in `F_a`, define

`Ext_a(w)_i = T_i<-a(w)`.

The v0.81 composition law gives exact compatibility. At the anchor,

`Ext_a(w)_a = T_a<-a(w) = w`.

Conversely, every compatible section satisfies

`s_i = T_i<-a(s_a)`.

Therefore evaluation at any selected anchor is an exact equivalence

`Gamma(F) ~= F_a`.

This is a finite pair-groupoid descent theorem, not a claim about arbitrary sheaves or continuum gauge systems.

## Root-independent evaluation

For the exact atlas chart `C_i`,

`ev_C_i(s_i) = ev_C_j(s_j)`

for all roots `i,j`. Thus each compatible section defines one exact group value independent of the chart used to evaluate it.

For every class function `chi`, the associated Wilson value is also root independent.

## Canonical global separator

The v0.81 route-0 normalized mixed word is `M = Y_3 D`. Its global extension is

`s^M_i = T_i<-0(M)`.

At every root:

- ordered AB evaluates to the identity permutation and has permutation trace `3`;
- ordered BA evaluates to `(021)` and has permutation trace `0`.

Hence the AB/BA separator is represented by one compatible global section rather than four separately transported expressions.

## Exact runtime ledger

The certificate records:

- 6 literature records;
- 160 global-section component records;
- 640 section-compatibility records;
- 40 anchor-reconstruction records;
- 4 anchor-fiber equivalence records;
- 640 section evaluation/Wilson descent records;
- 640 root-pair Wilson records;
- 4 canonical global-section separator records;
- 4 source-confidence preservation records;
- 4 memory-fusion records;
- 8 full-rank transport records;
- 4 singular atomic records;
- 3 rank-one source boundaries.

All source confidence values are preserved exactly: `1/3`, `5/18`, `11/54`, and `11/54`. The new global-section penalty is exactly zero.

## Fail-closed behavior

The checker rejects:

- any change to the regenerated v0.81 source certificate;
- global-section component or compatibility edits;
- anchor reconstruction or equivalence edits;
- evaluation or Wilson descent edits;
- separator edits;
- confidence edits;
- authority-boundary reversals;
- unexpected claims;
- collection count or digest mismatches.

## Authority boundary

MemoryOS v0.82 remains future-only, read-only, advisory, non-ranking, non-pruning, non-selecting, non-committing, non-activating, non-executing, non-mutating, non-verifying, and non-truth-authoritative.
