# KuuOS Root Map v0.45

This stage follows overview index v0.44.

It adds a compact map for the checked repository layers.

## Focused check

```bash
python3 -m unittest tests.test_kuuos_root_map_v0_45
```

## Current layer

The map names the current check, sequence, manifest index, docs index, overview index, and this root map.

This layer is read-only and metadata-only.
