# KuuOS Root Map Ledger v0.46

This stage follows root map v0.45.

It adds a compact ledger for the current root map checks.

## Focused check

```bash
python3 -m unittest tests.test_kuuos_root_map_ledger_v0_46
```

## Current layer

The ledger names the current check, current root sequence, root map, and this ledger.

This layer is read-only and metadata-only.
