# Checkpoint Namespace Compatibility v1.08

This document records the compatibility boundary between checkpoint references and existing repository transition interfaces.

Checkpoint references remain under the checkpoint namespace.

Branch-only interfaces are not compatible with checkpoint references.

The compatibility check is read-only and grants no execution authority.
