import KUOS.DependentOriginationCoreSpineV1_7
import KUOS.DependentOriginationHigherMulticategoryCoherenceV1_8

namespace KUOS.DependentOriginationCoreSpineV1_8

/-!
# Dependent-origination non-quantum core spine v1.8

The parent spine now carries two independent higher dimensions:

```text
path dimension:
  contextual transport
  -> bicategorical 2-morphisms and coherence

multi-condition dimension:
  positive-arity operations
  -> planar multicategorical grafting
  -> operation and expression two-cells
```

The current carrier semantics remains explicitly set-truncated when it maps
higher operation cells to equality.  No symmetric input action, monoidal process
structure, stack condition, or unbounded higher-categorical completion is
required by this aggregate.
-/

end KUOS.DependentOriginationCoreSpineV1_8
