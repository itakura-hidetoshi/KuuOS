import KUOS.DependentOriginationCoreSpineV1_9
import KUOS.DependentOriginationCausalMonoidalProcessV1_10

namespace KUOS.DependentOriginationCoreSpineV1_10

/-!
# Dependent-origination non-quantum core spine v1.10

The restored parent structure now exposes four deliberately distinct axes:

```text
contextual axis:
  context-indexed transport / descent / cofinal semantics

higher axis:
  bicategorical path coherence
  + higher multicategorical operation coherence

process axis:
  sequential composition
  + monoidal independent parallel composition

causal axis:
  orientation of admitted processes
  + categorical discarding / normalization
  + intervention closure
  + normalized-state preservation
```

Causality is not identified with Markovianity, and arbitrary multi-condition
operations are not assumed to be no-signalling.  The causal layer is additive
over the v1.9 monoidal process carrier and does not weaken any earlier theorem.
-/

end KUOS.DependentOriginationCoreSpineV1_10
