import KUOS.DependentOriginationScaledTerminalRLPV1_41

namespace KUOS.DependentOriginationCoreSpineV1_41

open CategoryTheory
open KUOS.DependentOriginationScaledTerminalRLPV1_41

/-!
# Dependent-origination core spine v1.41

The presentation-independence spine now reaches a genuine categorical lifting
property in the category of explicitly scaled simplicial sets.

The parent dependent-origination structure remains contextual establishment
plus composable transport.  The scaled-Duskin development is still a higher
specialization/completion, not a redefinition of that parent.

The v1.41 strict-fibrancy route is:

```text
coherent normalized bicategorical model equivalence
  -> canonical global Duskin prism
  -> presentation-independent homotopy-class horn filling

minimal scaling on canonical horn-cylinder attachments
  + simplex-projection scaling on cylinders
  -> native category of scaled simplicial sets
  -> HasLiftingProperty(attachment inclusion, target -> scaled point)
  -> globally scaled cylinder lift
  -> v1.40 attachment lifting
  -> v1.39 two-sided cylinder extension
  -> homotopy-class strictification
  -> presentation-independent strict scaled fibrancy.
```

No unscaled anodyne statement is identified with the scaled terminal RLP.
-/

end KUOS.DependentOriginationCoreSpineV1_41
