import KUOS.DependentOriginationCoreSpineV1_10
import KUOS.DependentOriginationMarginalNoSignallingV1_11

namespace KUOS.DependentOriginationCoreSpineV1_11

/-!
# Dependent-origination non-quantum core spine v1.11

The parent structural frontier now contains four distinct axes:

```text
contextual axis:
  composable transport / descent / cofinal semantics

higher axis:
  bicategorical path coherence
  + higher multicategorical operation coherence

process axis:
  sequential composition
  + monoidal parallel composition

causal axis:
  causal orientation
  + discard normalization
  + explicit tensor marginals
  + tensor-separated no-signalling
```

The v1.11 no-signalling theorem is intentionally restricted to local causal
interventions acting on separate tensor factors.  Arbitrary multi-condition
operations remain outside this theorem unless an additional marginal or
factorization law is supplied.
-/

end KUOS.DependentOriginationCoreSpineV1_11
