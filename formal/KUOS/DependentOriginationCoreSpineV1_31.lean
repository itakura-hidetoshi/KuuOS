import KUOS.DependentOriginationCoreSpineV1_30
import KUOS.DependentOriginationModelEquivalenceScaledHornPresentationV1_31

namespace KUOS.DependentOriginationCoreSpineV1_31

/-!
# Dependent-origination core spine v1.31

The parent contextual-transport definition remains unchanged.

Version 1.31 connects bicategorical model replacement to the v1.30
presentation-independent scaled-fibrancy invariant:

```text
bidirectional strictly-unitary model equivalence
  + full scaled global Duskin transport both ways
  + admissible horn-family preservation
  + round-trip filler equivalence
  -> ScaledHornPresentationEquivalence
  -> HasScaledHornFillers is invariant
```

The important boundary remains explicit: arbitrary v1.26
`BicategoricalModelEquivalence` is not yet automatically promoted to this
scaled-Duskin package.  Normalization, coherent quasi-inverse data, and full
scaling are the remaining construction problem.
-/

end KUOS.DependentOriginationCoreSpineV1_31
